void main() {
  var names = ["Mg Mg", "Aung Aung", "Tun Tun", "Su SU"];
  names.forEach((element) => print(element));
  // names.forEach((names)=>print(names));
}

/*
void main(
  List<String> params,
) // => this is adding argument at main function parameter holder ()
{
  print(params); // -> []
  print(params.length);
  // -> at terminal if run dart main.dart One Two Three -> it will output 3.
  print(params[1]); // -> Two
  print("This is main function and being called by Dart Program");
} */
