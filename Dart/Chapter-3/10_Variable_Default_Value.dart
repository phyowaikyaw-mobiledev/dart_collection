void main() {
  int age = 20; // defind or  assign
  age = 40; // re-defind or re-assign
  age = 60;

  print(age);

  String name; // declare
  name = "Andrew"; //assign
  print(name);
}

// Default value of all variable is null value but we must assign the value at the latest version of dart.
